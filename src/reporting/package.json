{
  "name": "@psyai/reporting",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist",
    "README.md"
  ],
  "dependencies": {
    "@psyai/contracts": "file:../contracts",
    "@psyai/counseling": "file:../counseling",
    "@psyai/resonance": "file:../resonance",
    "@psyai/simulation": "file:../simulation",
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "typescript": "^5.4.5"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "pretest": "npm run build --silent",
    "test": "node ./tests/run-reporting-tests.mjs"
  }
}
