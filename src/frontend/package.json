{
  "name": "@psyai/frontend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist",
    "README.md"
  ],
  "dependencies": {
    "@psyai/app-state": "file:../app-state",
    "@psyai/contracts": "file:../contracts",
    "@psyai/shared": "file:../shared",
    "react": "^19.2.6",
    "react-dom": "^19.2.6"
  },
  "devDependencies": {
    "@types/react": "^19.2.14",
    "@types/react-dom": "^19.2.3",
    "typescript": "^5.9.3"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "pretest": "npm run build --silent",
    "test": "node ./tests/run-frontend-tests.mjs",
    "validate:frontend": "npm run build --silent && node ./validate-frontend.mjs"
  }
}
